<h1>Queries for channel <%= @channel.name %></h1> 

<ul>
  <% for f in @feeds %>
    <li class="arrow">
      <em><%= f.id %></em>
      <%= link_to f.name, channel_feed_path(@channel, f) %>
      <!--<em><%= @channel.name %></em>-->
      <!--<%= link_to 'Destroy', channel_feed_path(@channel, f), confirm: 'Are you sure?', method: :delete %>-->
    </li>
  <% end %>
</ul>

<ul><li><%= link_to 'Add New Query', new_channel_feed_path(@channel) %></li></ul>
<ul><li><%= link_to 'Back to Channel', @channel %></ul></li>
