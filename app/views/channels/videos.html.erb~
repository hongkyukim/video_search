<p id="notice"><%= notice %></p>

<h1><%= @channel.name %></h1>

<table>
  <tr>
    <th>Photo</th>
    <th>Id</th> 
    <th>Title</th>
    <th>Provider</th>
    <!--<th>VideoId</th>-->
    <th>Duration</th>
  </tr>

<% @videos.each do |v| %>
  <tr>
    <td><a href="http://www.youtube.com/watch?v=<%= v.yt_video_id %>&feature=youtube_it" target="_blank"><%= image_tag v.thumbnail_url, :size => "60x45" if v.thumbnail_url %></a></td>
    <td><%= v.id %></td>
    <td><%= link_to v.title, video_path(v) %></td>
    <td><%= v.provider %></td>
    <!--<td><%= v.yt_video_id %></td>-->
    <td><%= v.duration %></td>
  </tr>
<% end %>
</table>

<br />
<%= link_to 'Go To All Channels', channels_path %> 
<!--|
<%= link_to 'Add New video', new_video_path %>-->

<% if user_signed_in? %>
<% if current_user %>
| <%= link_to 'Go to My channels', user_channels_path(current_user) %>
<% end %>
<% end %>




