<h1>Editing user</h1>

<%= render 'form' %>

<%= link_to 'Show', @user %> 
   
<% if user_signed_in? %>
  <% if current_user.admin? %>
       | <%= link_to 'Back to Users', users_path %>
  <% end %>
<% end %>
